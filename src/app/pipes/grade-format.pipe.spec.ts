import { GradeFormatPipe } from './grade-format.pipe';

describe('GradeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new GradeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
